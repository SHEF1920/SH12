#include <stdio.h>
#include <math.h>

int main()
{
    float z1 = 0, z2 = 0, a = 0;
    scanf("%f", &a);
    z1 = (sin(2 * a) + sin(5 * a) - sin(3 * a)) / (cos(a) + 1 - 2 * pow(sin(2 * a), 2));
    printf("%f\n", z1);
    z2 = 2 * sin(a);
    printf("%f", z2);
    return 0;
}
